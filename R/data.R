#' Lookup table for vulnerability factor codes
#'
#' Lookup table for vulnerability factor questions, codes, maximum value, and
#' whether the factor is calculated by the spatial analysis.

"vulnq_code_lu_tbl"

#' Combined index lookup table
#'
#' A table for combining index scores from the sensitivity (B/C) and modelled response
#' to climate change (D) sections
"comb_index_tbl"
