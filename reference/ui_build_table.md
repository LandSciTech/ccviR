# Table to build the UI

A table for identifying the update function to apply to each input. May
get more uses in the future

## Usage

``` r
ui_build_table
```

## Format

An object of class `data.frame` with 48 rows and 12 columns.
