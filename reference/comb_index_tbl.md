# Combined index lookup table

A table for combining index scores from the sensitivity (B/C) and
modelled response to climate change (D) sections

## Usage

``` r
comb_index_tbl
```

## Format

An object of class `tbl_df` (inherits from `tbl`, `data.frame`) with 25
rows and 3 columns.
