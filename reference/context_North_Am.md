# Contextual map data for North America

Data used to build context map in pdf report.

## Usage

``` r
context_North_Am
```

## Format

An object of class `sf` (inherits from `data.frame`) with 64 rows and 2
columns.
