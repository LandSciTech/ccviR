# Lookup table for vulnerability factor codes

Lookup table for vulnerability factor questions, codes, maximum value,
and whether the factor is calculated by the spatial analysis.

## Usage

``` r
vulnq_code_lu_tbl
```

## Format

An object of class `data.frame` with 27 rows and 4 columns.
